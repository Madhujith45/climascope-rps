# ClimaScope – Edge Layer Dependencies (Raspberry Pi Hardware)
# Install on Raspberry Pi OS (Bookworm / Bullseye) with:
#   pip install -r requirements-pi.txt

# Include common development dependencies
-r requirements-dev.txt

# ── Sensor hardware drivers ───────────────────────────────────────────────────
# RPi.GPIO / I2C interfaces must be enabled in raspi-config
adafruit-circuitpython-dht==4.0.5
adafruit-circuitpython-bmp280==3.3.6
adafruit-circuitpython-ads1x15==2.4.4

# adafruit-blinka provides board/busio abstractions for CircuitPython on Pi
adafruit-blinka==8.43.0
